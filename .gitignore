# Build output
*.obj
*.exe
*.pdb
*.ilk
*.lib
*.exp
*.res
build/
# Editor / OS
.vs/
*.user
Thumbs.db
# Claude local (machine-specific) settings and worktrees
.claude/settings.local.json
.worktrees/
# Runtime config copy (generated next to the exe by LoadConfig on first run)
/magnifier.ini
# Diagnostic logs
*.log
# PresentMon / perf captures
*.csv
# WebView2 user-data folder (created next to WindConfig.exe at runtime)
*.WebView2/
# Self-test / diagnostic dumps
wind_selftest.png
wind_hdr_diag.txt
# Stray build-log redirects
build_*.txt
.superpowers/

# graphify knowledge-graph output (generated)
graphify-out/

# Installer: release artifacts and generated media (regenerate with
# installer/make-loop.mjs and installer/make-over.mjs)
dist/
installer/media/
# ...but the WebView2 bootstrapper stub IS committed; it is a Microsoft
# redistributable the installer packs, not build output. Overrides *.exe above.
!installer/MicrosoftEdgeWebview2Setup.exe
